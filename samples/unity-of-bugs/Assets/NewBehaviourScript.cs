using Sentry.Unity;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        _ = new ReplacementClass();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
